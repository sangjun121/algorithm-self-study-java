# Set 자료구조

## 주요 문법

### 1. Set 정렬
- set 정렬시 list로 변환 후, Collections.sort()사용
```java
private static Set<Integer> result = new HashSet<>();

public static void main(String[] args) {
    List<Integer> resultResult = new ArrayList<>(result);
    Collections.sort(resultResult);
}
```
