# 배열 (Array)

## 기본 문법
1. 이차원 배열에서 한줄(행)만 분리해서 넘겨주기 
   - 주의. 한 열(세로)를 분리하는 것은 수동으로 해주어야 함. (for문 사용)
   
```java
int[][] arr; //일때,
int[] firstRowOfArr = arr[0]; // 첫번째 행 분리
```

## 기타

### 1. 2차원 배열 대각선 순회하기
i+j < K 이용하기
