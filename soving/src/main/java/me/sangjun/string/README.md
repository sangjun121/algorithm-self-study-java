## 백준 2744번 대소문자 바꾸기

### 복기사항
1. 문자열 각 토큰 분리할 때 사용 가능한 메소드 : ```char[] chars = input.toCharArray();```
2. 문자열 아스키코드 다루기 : 산수 후에, (char)으로 형 변환 

## 백준 1919 애너그램 만들기
1. 문제 해결 아이디어: 모든 알파벳에 대한 등장 횟수 배열을 만들고, 각 원소 칸을 비교한 차이만큼을 모두 합산한다.
2. 총 알파벳의 개수는 26개, 자바에서 빈배열 초기화 시에는 모두 0으로 초기화 된다.
3. 절대값 메소드 Math.abs()

## 백준 1543 문서 검색
1. 문제 해결 아이디어는 String의 indexOf 메소드를 사용한 검색
2. 따라서 해당 함수의 시간복잡도에 대한 이해가 필요 (최악의 경우: O(N × M): N = 문서길이, M = 단어길이)
3. 추후 시간될 때, 해당 함수 생으로 구현해보는 연습 필요

## 백준 13223 소금 투하
1. 반례찾기가 중요했던 문제
2. 두 시간이 모두 00:00:00으로 동일한 경우에 00:00:00 출력이 아닌 24:00:00으로 취급
3. 이는 출력 조건에 명시되어 있다. 문제 꼼꼼히 읽기
