## 백준 2579 계단 오르기
- DP로 문제 풀이
- 점화식을 찾아낼때 조건하나가 더 붙는 경우. study자료의 2번 유형에 해당
- Dy[N] = **Dy[N-2] + 마지막계단** 과 **Dy[N-3] + 마지막계단 직전 계단 + 마지막 계단** 중 최대 값
- DP 문제 풀이에서 중요한 것은, 먼저 완탐이 안됨을 깨닫고 DP로 해결해보자라는 방향성을 가져야함
- 따라서, 완탐의 시간복잡도와 공간 복잡도를 계산해보는 것도 필요.
- 또한, 사용하는 자료형의 사이즈를 계산해보는것도 필요. 정답의 최대치는 모든 계단을 하나씩 다 밟는다고 가정하면 총 N* 최대 점수이므로 300만. 즉 Integer 범위에 들어온다.
- 자바의 Int 범위는 최소값 약 -21억 ~ 21억까지 표현 가능
- 문제 풀이에서 막혔던 포인트: n = 1,2,3일때에 대한 출력 정의를 해주지 않았기에 컴파일 에러 발생

## 백준 11057 오르막 수
- DP로 문제 풀이
- 자료형 선택: 결과값을 저장할 타입으로 integer 사용 가능. 결국 중간 연산부터 나머지연산 사용시 10007이하의 범위 필요.
- 길이가 커질수록 경우의 수가 기하급수적으로 증가하므로 완전탐색은 불가능하다고 판단
- 길이 N, 맨 처음 숫자 i(0~9)인 오르막 수의 개수를 DP[i+1행][N열]로 정의 (행이 하나 더 많은 이유는 각 N에 대하여 최종합을 저장하기 위한 저장소)
- 오르막 조건(이전 자리 ≤ 현재 자리)을 DP 상태로 분리하여 점화식 구성
- 문제 풀이에서 막혔던 핵심 포인트는 DP 값이 매우 커진다는 점을 간과한 것
- 모듈러 연산을 마지막에만 적용하여 중간 계산 과정에서 int 오버플로우 발생
- 해결 방법은 DP 값을 계산하는 중간 단계부터 %10007을 적용하는 것
- 교훈: 모듈러가 있는 DP 문제는 **결과 보정이 아니라 overflow 방지를 위해 중간 계산부터 mod 적용**

## ⭐️ 백준 12865번 평범한 배낭
- 완전탐색 사용시 O(2^100)의 시간 소요. (약 1,200억의 10억 배) 따라서 불가능
- 아이디어: 각 물건이 들어가는지 아닌지로 sub문제 분리
- 대표적인 유형으로 암기하자!

## 백준 11066번 파일 합치기
- 완전 탐색으로 대략적인 시간 복잡도를 계산하면, 처음 선택가능한 파일 수 N개, 그 이후 N-1 ...로 대략 O(K!)만큼의 시간 복잡도가 걸린다. 따라서 DP를 고려해볼 수 있다.
- 구간 L~R에 대한 문제를 해결하는 아이디어로, i~j사이의 구간 최소합을 구하고(sub문제) 1~K인 경우를 찾기(진짜문제)  
- 정의. dy[i][j] 는 i에서 j 구간의 최소합을 의미.
- 점화식. dy[i][j] = min{ Dy[i][k] + Dy[k+1][j] + (i~j 파일의 총량) }
- 여기서 k는 i ~ j-1인 모든 자연수를 의미. 모든 k에 대한 위의 점화식 최소가 dy[i][j]가 된다.
- sum[i][j]는 i번에서 j번 파일의 총량을 의미한다. (점화식에서 마지막에 더해지는 값을 의미)
  - 해당 연산은 PrefixSum(구간합)을 사용하면 간단하게 구할 수 있다.
- 해당 문제는 배열(dy[i][j])을 채우는 순서가 매우 중요하다. 윗줄부터 차례로 채우면 안되고, 대각선으로 채워야 한다.
  - 따라서 2중 for문의 순회의 접근을 i와 j로 규정하지 않고, i와 len(작은순서대로)로 정하고, i+len으로 j를 표현하는 방식으로 순회하자.
- 최종적인 시간복잡도는 sum배열은 O(N^2)로 전처리, Dy배열은 O(N^3)의 시간이 걸린다.

## 백준 15681번 트리와 쿼리
- 빈출 유형! [유형5. 트리 구조에서 문제를 해결할 때](../../../../../../../study/dynamicProgramming/README.md)
- 트리의 sub 트리의 정점 개수를 구하는 문제
- 해당 문제는 tree 정보를 부모, 자식 관계로 표현하는 함수(makeTree)와 서브 트리의 정점개수를 구하는 함수(countNodeOfSubTree)를 사용
- 핵심! **트리에서 DP는 항상 DFS를 사용한다고 생각해도 무방하다!**
- 위 두함수는 기본 개념이므로 암기하자. (자세한 함수식은 [study/graph/README.md](../../../../../../../study/graph/README.md) 참조)
